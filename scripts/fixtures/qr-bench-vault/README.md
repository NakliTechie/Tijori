# qr-bench-vault

Deterministic Tijori v2 vault, generated by `scripts/make-test-vault.mjs` (seed `0x7a1b0c9d`).
Password: `qr-bench-2026`. 21 events (20 entries: 7 login, 4 code, 3 card, 6 note).
Archive as the QR sender serialises it: `archive.json`, 20918 bytes (20.4 KB).

Fixed payload for the transport benchmarks in `plan/qr-benchmarks.md`. Regenerate with the script; `--check` diffs.
