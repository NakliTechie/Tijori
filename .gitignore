*
!index.html
!README.md
!ARCHITECTURE.md
!LICENSE
!.gitignore
!guide/
!guide/**
!scripts/
!scripts/**
!.github/
!.github/**
# dev-only checks and their one decoder dependency (node_modules stays ignored)
!package.json
!package-lock.json

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# macOS finder metadata — re-excluded after guide/** whitelist
**/.DS_Store

# local-only working notes (already covered by the wildcard above, listed
# explicitly so the intent survives future edits)
plan/
